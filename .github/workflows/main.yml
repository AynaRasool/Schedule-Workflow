name: Scheduled Job

on:
  schedule:
    - cron: "*/2 * * * *"   # runs every 2 minutes

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Custom Message
        run: echo "Ayna’s scheduled workflow is running automatically 🚀"

      - name: Show time
        run: date
